# クイック実行手順

## 🚀 最短手順（サーバー側で直接実行）

### 1. サーバーにSSH接続

**Git Bashで実行：**
```bash
ssh xserver-besttrust
```

**または直接接続：**
```bash
ssh -i ~/.ssh/id_rsa -p 10022 besttrust@sv16.sixcore.ne.jp
```

### 2. サーバー側で統合スクリプトを実行

サーバーに接続後、以下のコマンドを実行：

```bash
# スクリプトをダウンロードまたは作成
cd ~
cat > server_setup_commands.sh << 'SCRIPT_EOF'
```

その後、`server_setup_commands.sh`の内容をコピー＆ペーストして、最後に以下を入力：

```bash
SCRIPT_EOF

# 実行権限を付与
chmod +x server_setup_commands.sh

# スクリプトを実行
bash server_setup_commands.sh
```

### 3. 動作確認

スクリプト実行後、ブラウザで以下にアクセス：

1. **テストファイル**: https://resend.kimito-link.com/_test.txt
2. **デバッグモード**: https://resend.kimito-link.com/?dbg=raw
3. **通常アクセス**: https://resend.kimito-link.com/

---

## 📋 詳細手順

詳細な手順については、`EXECUTE_PLAN_GUIDE.md`を参照してください。

---

## ⚠️ 注意事項

- スクリプト実行前に、既存のディレクトリは自動的にバックアップされます
- SSH鍵（`id_kimitolink`）がサーバーに存在しない場合は、ローカルからコピーする必要があります
- 実行中にエラーが発生した場合は、`EXECUTE_PLAN_GUIDE.md`のトラブルシューティングセクションを参照してください

---

## 📁 関連ファイル

- `server_setup_commands.sh` - サーバー側で実行する統合スクリプト
- `fix_page_not_found.sh` - Page Not Found修正スクリプト（個別実行用）
- `EXECUTE_PLAN_GUIDE.md` - 詳細な実行ガイド
- `SSH_INFO.md` - SSH接続情報
